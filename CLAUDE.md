Architecture decisions and layer conventions: `.claude/rules/architecture.md`,
`.claude/rules/ddd.md`.

Code style (incl. file naming), doc style: `.claude/rules/code-style.md`,
`.claude/rules/doc-style.md`.

Project overview and how to run it: `README.md`. Research sources behind the design decisions:
`temp/source.md`.

The folder layout is no longer specced anywhere on purpose — every layer now has real code, so
follow the existing feature's pattern (`todo-lists/`) when adding a new one.
